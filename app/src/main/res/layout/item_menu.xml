<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
    >

    <data>

        <variable
            name="item"
            type="com.wcs.vcc.main.vo.MenuItem"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal">

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/txt_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawablePadding="4dp"
                android:ellipsize="end"
                android:gravity="center_horizontal"
                android:lines="2"
                android:paddingBottom="10dp"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:text="@{item.name}"
                android:textColor="@android:color/primary_text_light"
                android:textSize="14sp"
                tools:drawableTop="@drawable/ic_menu_item_01"
                tools:text="BigC"/>

            <TextView
                android:id="@+id/tv_number"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="end"
                android:layout_marginRight="15dp"
                android:background="@drawable/icon_notification"
                android:gravity="center"
                android:text="@{String.valueOf(item.notification)}"
                android:textColor="#FFF"
                android:textSize="12sp"
                app:visibleGone="@{item.notification &gt; 0}"
                tools:text="99"
                tools:visibility="visible"/>
        </FrameLayout>
    </LinearLayout>

</layout>